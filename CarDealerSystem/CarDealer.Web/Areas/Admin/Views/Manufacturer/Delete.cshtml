@model CarDealer.Services.Models.Manufacturer.ManufacturerUpdateServiceModel

<h2 class="text-md-center">Delete @Model.Name</h2>

<h3 class="text-md-center">Are you sure you want to delete this?</h3>
<div class="container text-md-center">
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <form asp-action="Delete" style="margin-bottom: 15px">
        <input type="text" name="name" value="@Model.Name" hidden="hidden" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-default">Back to List</a>
    </form>
</div>
